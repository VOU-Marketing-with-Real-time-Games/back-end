spring:
  application:
     name: notification-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: notification_db
server:
  port: 8002

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true