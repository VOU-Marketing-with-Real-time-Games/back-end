spring:
  application:
     name: notification-service
  data:
    mongodb:
      host: ${MONGO_HOST}
      port: 27017
      database: ${NOTIFICATION_DB}
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8002

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true