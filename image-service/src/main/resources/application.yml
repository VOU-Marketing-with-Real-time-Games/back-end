spring:
  application:
    name: image-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: image_db
server:
  port: 8007

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true