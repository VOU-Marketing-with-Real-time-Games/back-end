spring:
  application:
    name: brand-service
  datasource:
    url: jdbc:mysql://${MYSQL_URL}:3306/${BRAND_DB}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.hibernate.format_sql: true

server:
  port: 8003

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true