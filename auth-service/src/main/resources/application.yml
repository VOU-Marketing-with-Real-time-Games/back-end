spring:
  application:
    name: auth-service
server:
  port: 9004
jwt:
  secret: ${JWT_SECRET:your-secret-key}
  expiration: ${JWT_EXPIRATION:10000}
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
